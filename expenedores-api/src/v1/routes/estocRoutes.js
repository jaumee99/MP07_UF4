/*
const express = require("express");
const router = express.Router();
const estocController = require("../../controllers/estocController");

router
    .get("/", estocController.getAllEstocs)
    .get("/:estocID", estocController.getEstoc)
    .post("/", estocController.createEstoc)
    .patch("/:estocID", estocController.updateEstoc)
    .delete("/:estocID", estocController.deleteEstoc);

module.exports = router;
*/